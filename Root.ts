/* declare rootDir = __dirname

import path from 'path';

/* const rootDir = path.resolve(__dirname, '../');
console.log(rootDir);  */
export default class Root{
	public static rootDir:string = __dirname
}
