/*
const math = require('mathjs');

// 定义要计算的角度
const angle1 = math.unit('154 degrees 2 minutes');
const angle2 = math.unit('34 degrees 6 minutes');

// 计算角度的差值
const difference = math.subtract(angle1, angle2);

// 获取结果的度数和分数
const degrees = math.floor(math.number(difference));
const minutes = math.round(math.number(math.subtract(difference, degrees)), 2);

// 输出结果
console.log(`差值为：${degrees}度${minutes}分`);*/
